Using C++ 20 moudles and `import std`, with cmake

### Notes

Currently, clangd only works with clang.

I have tried using gcc with .clangd to hide some errors, unluckily it doesn't work, completion is totally broken